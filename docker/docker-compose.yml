services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../config/.env
    volumes:
      - ../data:/app/data
      - ../config:/app/config
    restart: unless-stopped
